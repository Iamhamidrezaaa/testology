# 🎯 سیستم گیمیفیکیشن و پنل درمانگر - Testology

## 📋 خلاصه

این مستند توضیح می‌دهد که چگونه سیستم گیمیفیکیشن (XP, Level, Achievements) و پنل مدیریت درمانگر در پروژه Testology پیاده‌سازی شده است.

---

## 🎮 بخش اول: سیستم گیمیفیکیشن (XP & Leveling)

### 🗃️ مدل دیتابیس (Prisma Schema)

```prisma
model UserProgress {
  id           String   @id @default(cuid())
  userId       String   @unique
  user         User     @relation(fields: [userId], references: [id])
  xp           Int      @default(0)
  level        Int      @default(1)
  achievements Json     @default("[]")
  totalTests   Int      @default(0)
  streakDays   Int      @default(0)
  lastActivity DateTime @default(now())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
```

### 🔢 سیستم محاسبه XP و Level

**فرمول محاسبه سطح:**
```typescript
Level = floor(1 + sqrt(XP / 100))
```

**XP مورد نیاز برای سطح بعدی:**
```typescript
nextLevelXP = (currentLevel + 1)² × 100
```

**XP از تست:**
- هر امتیاز تست = 10 XP
- مثال: تست با امتیاز 75 = 750 XP

### 📊 دستاوردها (Achievements)

#### دستاوردهای تعداد تست:
- `first_test`: اولین تست ✅
- `test_enthusiast`: 5 تست 📚
- `test_master`: 10 تست 🎓
- `psychology_expert`: 25 تست 🧠
- `test_legend`: 50 تست 👑

#### دستاوردهای سطح:
- `level_5`: رسیدن به سطح 5 ⭐
- `level_10`: رسیدن به سطح 10 🌟
- `level_20`: رسیدن به سطح 20 💫
- `level_50`: رسیدن به سطح 50 ✨

#### دستاوردهای XP:
- `xp_1k`: 1,000 XP 💎
- `xp_5k`: 5,000 XP 💍
- `xp_10k`: 10,000 XP 👸

#### دستاوردهای تداوم:
- `streak_3`: 3 روز متوالی 🔥
- `streak_week`: 7 روز متوالی 🚀
- `streak_month`: 30 روز متوالی 🏆
- `streak_100`: 100 روز متوالی 🎖️

---

## 🔌 API Endpoints

### 1️⃣ افزودن XP پس از تست
**POST** `/api/xp/add`

**Request Body:**
```json
{
  "score": 75,
  "testSlug": "phq9"
}
```

**Response:**
```json
{
  "success": true,
  "xp": 1250,
  "level": 4,
  "xpAdded": 750,
  "leveledUp": true,
  "newAchievements": ["level_5", "test_master"]
}
```

**ویژگی‌ها:**
- محاسبه خودکار XP
- بررسی level up
- شناسایی دستاوردهای جدید
- ارسال نوتیفیکیشن برای level up و achievements

### 2️⃣ دریافت پیشرفت کاربر
**GET** `/api/xp/progress`

**Response:**
```json
{
  "xp": 1250,
  "level": 4,
  "progress": 65.5,
  "nextLevelXP": 2500,
  "totalTests": 12,
  "streakDays": 7,
  "achievements": ["first_test", "test_enthusiast", "level_5"],
  "lastActivity": "2024-01-15T10:30:00Z"
}
```

---

## 👨‍⚕️ بخش دوم: سیستم درمانگر

### 🗃️ مدل دیتابیس

```prisma
model TherapistPatient {
  id          String   @id @default(cuid())
  therapistId String
  patientId   String
  status      String   @default("active")
  notes       String?
  startDate   DateTime @default(now())
  endDate     DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([therapistId, patientId])
}
```

### 🔌 API Endpoints برای درمانگر

#### 1️⃣ دریافت لیست بیماران
**GET** `/api/therapist/patients`

**Response:**
```json
{
  "success": true,
  "patients": [
    {
      "id": "clx...",
      "patientId": "user123",
      "status": "active",
      "notes": "نیاز به پیگیری هفتگی",
      "user": {
        "id": "user123",
        "name": "علی احمدی",
        "email": "ali@example.com"
      },
      "recentTests": [...],
      "progress": {
        "xp": 2500,
        "level": 6,
        "totalTests": 15,
        "streakDays": 12
      }
    }
  ]
}
```

#### 2️⃣ افزودن بیمار جدید
**POST** `/api/therapist/patients`

**Request Body:**
```json
{
  "patientId": "user123",
  "notes": "بیمار جدید - نیاز به ارزیابی اولیه"
}
```

#### 3️⃣ دریافت اطلاعات کامل بیمار
**GET** `/api/therapist/patients/[patientId]`

**Response:**
```json
{
  "success": true,
  "patient": {
    "id": "user123",
    "name": "علی احمدی",
    "email": "ali@example.com",
    "tests": [...],
    "progress": {...},
    "sessions": [...],
    "notes": "یادداشت‌های درمانگر",
    "analysis": {
      "summary": "15 تست انجام شده - 2 مورد نیاز به توجه ویژه",
      "riskLevel": "medium",
      "totalTests": 15,
      "highRiskTests": 2,
      "recommendations": [...]
    }
  }
}
```

#### 4️⃣ به‌روزرسانی یادداشت بیمار
**PATCH** `/api/therapist/patients/[patientId]`

**Request Body:**
```json
{
  "notes": "پیشرفت خوبی داشته - ادامه جلسات دوهفته‌ای",
  "status": "active"
}
```

#### 5️⃣ مدیریت جلسات درمانی
**GET/POST** `/api/therapist/sessions`

**POST Request:**
```json
{
  "patientId": "user123",
  "date": "2024-01-20T15:00:00Z",
  "duration": 60,
  "note": "جلسه اول - ارزیابی",
  "meetingLink": "https://meet.google.com/abc-xyz"
}
```

---

## 📄 بخش سوم: گزارش PDF

### 🔌 API Endpoints

#### 1️⃣ گزارش یک تست
**GET** `/api/report/[testId]`

**دانلود:** `testology-report-{testId}.pdf`

**محتوا:**
- اطلاعات تست
- نمرات و تحلیل
- توصیه‌ها

#### 2️⃣ گزارش جامع
**GET** `/api/report/comprehensive?userId=user123`

**دانلود:** `testology-comprehensive-report-{userId}.pdf`

**محتوا:**
- خلاصه کامل تمام تست‌ها
- نمودار پیشرفت
- تحلیل ترکیبی
- توصیه‌های کلی

---

## 🎨 کامپوننت‌های React

### 1️⃣ ProgressTracker
**مسیر:** `components/dashboard/ProgressTracker.tsx`

**استفاده:**
```tsx
import { ProgressTracker } from '@/components/dashboard/ProgressTracker';

<ProgressTracker />
```

**نمایش:**
- سطح فعلی
- نوار پیشرفت
- XP / NextLevelXP
- آمار (تست‌ها، تداوم، دستاوردها)
- لیست دستاوردها

### 2️⃣ PatientsList
**مسیر:** `components/therapist/PatientsList.tsx`

**استفاده:**
```tsx
import { PatientsList } from '@/components/therapist/PatientsList';

<PatientsList />
```

**نمایش:**
- لیست بیماران
- آمار سریع هر بیمار
- آخرین تست‌ها
- لینک به صفحه جزئیات

---

## 🚀 صفحات Next.js

### 1️⃣ داشبورد پیشرفت
**مسیر:** `/dashboard/progress`
**فایل:** `app/dashboard/progress/page.tsx`

### 2️⃣ لیست بیماران درمانگر
**مسیر:** `/therapist/patients`
**فایل:** `app/therapist/patients/page.tsx`

---

## 🔐 امنیت و دسترسی

### محدودیت‌های دسترسی:

1. **API های XP:**
   - فقط کاربر لاگین شده
   - فقط برای خودش

2. **API های درمانگر:**
   - فقط کاربران با نقش `therapist` یا `admin`
   - دسترسی به بیماران خودشان

3. **API های گزارش:**
   - کاربر به گزارش خودش
   - درمانگر به گزارش بیماران خود
   - ادمین به همه

---

## 📊 نحوه استفاده

### برای کاربران عادی:

1. انجام تست‌های روان‌شناسی
2. خودکار دریافت XP
3. افزایش سطح و کسب دستاورد
4. مشاهده پیشرفت در `/dashboard/progress`

### برای درمانگران:

1. افزودن بیماران از پنل
2. مشاهده لیست و آمار در `/therapist/patients`
3. بررسی تست‌ها و تحلیل‌ها
4. ثبت یادداشت و برنامه‌ریزی جلسات
5. دانلود گزارش‌های PDF

---

## 🛠️ نصب و راه‌اندازی

### 1. نصب پکیج‌ها (قبلاً نصب شده):
```bash
npm install pdf-lib
```

### 2. اعمال تغییرات دیتابیس:
```bash
npx prisma db push
npx prisma generate
```

### 3. اجرای پروژه:
```bash
npm run dev
```

---

## 🎯 ویژگی‌های آینده (پیشنهادی)

- [ ] سیستم رتبه‌بندی (Leaderboard)
- [ ] دستاوردهای اختصاصی برای هر تست
- [ ] XP بونوس برای تداوم روزانه
- [ ] سیستم پاداش (Rewards)
- [ ] نمودارهای پیشرفت تصویری
- [ ] مقایسه با سایر کاربران
- [ ] چالش‌های هفتگی/ماهانه

---

## 📝 نکات مهم

1. **SQLite Limitation:** فیلد `achievements` از نوع `Json` است چون SQLite از آرایه String پشتیبانی نمی‌کند.

2. **Notification System:** هر level up و achievement جدید، نوتیفیکیشن ارسال می‌شود.

3. **Therapist Assignment:** رابطه بین درمانگر و بیمار از طریق `TherapistPatient` مدیریت می‌شود.

4. **PDF Generation:** از کتابخانه `pdf-lib` استفاده می‌شود (فونت فارسی در نسخه‌های بعدی).

---

## 🎉 نتیجه

سیستم گیمیفیکیشن و پنل درمانگر با موفقیت پیاده‌سازی شد و شامل:

✅ سیستم XP و Leveling کامل
✅ دستاوردها (19 مورد)
✅ پنل مدیریت بیماران برای درمانگر
✅ تحلیل ترکیبی تست‌ها
✅ گزارش PDF (تکی و جامع)
✅ کامپوننت‌های React زیبا
✅ API های RESTful کامل
✅ امنیت و محدودیت دسترسی

**پروژه Testology آماده برای ارائه سرویس‌های حرفه‌ای روان‌شناسی است! 🚀🧠**
















