# 👁️ AI Evaluator Dashboard - راهنمای کامل

## 🎯 مقدمه

Testology حالا دارای یک **سیستم ارزیابی خودکار** است که قابلیت‌های زیر را ارائه می‌دهد:

- **👁️ خودآگاهی**: مدل خودش خودش رو می‌بینه و ارزیابی می‌کنه
- **📊 تحلیل عملکرد**: نمایش دقیق دقت و خطاها
- **🔥 Heatmap تصویری**: نمایش عملکرد دسته‌ای
- **🧠 پیشنهاد بهبود**: AI Supervisor برای اصلاح مدل
- **📈 نمودارهای پیشرفته**: F1-Score، Precision، Recall

## ✨ ویژگی‌های کلیدی

- **🔍 ارزیابی خودکار**: تحلیل کامل دقت و خطاها
- **📊 Heatmap ماتریس سردرگمی**: نمایش تصویری عملکرد
- **📈 نمودار F1-Score**: عملکرد مدل به تفکیک دسته‌ها
- **🧠 تحلیل AI Supervisor**: پیشنهاد بهبود خودکار
- **💡 پیشنهادات هوشمند**: شناسایی نقاط ضعف و راه‌حل
- **📊 Cross-Validation**: ارزیابی قابلیت اطمینان مدل

## 🏗️ معماری سیستم

```
┌─────────────────────────────────────┐
│        AI Evaluator Dashboard       │
├─────────────────────────────────────┤
│  ┌─────────────┬─────────────────┐  │
│  │   Model     │   Data          │  │
│  │   Evaluation│   Analysis      │  │
│  └─────────────┴─────────────────┘  │
│  ┌─────────────┬─────────────────┐  │
│  │   Heatmap   │   Charts        │  │
│  │   Display   │   & Graphs      │  │
│  └─────────────┴─────────────────┘  │
│  ┌─────────────┬─────────────────┐  │
│  │   AI        │   Suggestions   │  │
│  │   Supervisor│   & Insights    │  │
│  └─────────────┴─────────────────┘  │
└─────────────────────────────────────┘
```

## 🚀 راه‌اندازی سریع

### 1. نصب و راه‌اندازی

```bash
# راه‌اندازی کامل سیستم
bash setup-ai-brain.sh

# تست سیستم ارزیابی
cd ml && python utils/evaluate_model.py
```

### 2. اجرای ارزیابی

```bash
# ارزیابی دستی
cd ml && python utils/evaluate_model.py

# یا از طریق API
curl -X POST http://localhost:3000/api/admin/ai/evaluate
```

### 3. مشاهده نتایج

```
دسترسی: /admin/ai-dashboard
```

## 🔧 کامپوننت‌های اصلی

### 1. اسکریپت ارزیابی مدل

**فایل**: `ml/utils/evaluate_model.py`

```python
def evaluate_model():
    # بارگذاری مدل و داده‌ها
    model, le, scaler, df = load_model_and_data()
    
    # پیش‌پردازش
    X, y, df_processed = preprocess_for_evaluation(df, le, scaler)
    
    # پیش‌بینی
    y_pred = model.predict(X)
    y_proba = model.predict_proba(X)
    
    # محاسبه متریک‌ها
    metrics = calculate_advanced_metrics(y, y_pred, y_proba)
    
    # تحلیل عملکرد
    suggestions = analyze_model_performance(metrics)
    
    # تولید Heatmap
    heatmap_data = generate_heatmap_data(conf_matrix, labels)
```

**ویژگی‌ها**:
- ارزیابی کامل مدل با متریک‌های پیشرفته
- تحلیل ماتریس سردرگمی
- محاسبه F1-Score، Precision، Recall
- Cross-Validation برای اطمینان
- تولید پیشنهادات بهبود

### 2. API ارزیابی

**فایل**: `app/api/admin/ai/evaluate/route.ts`

```typescript
export async function POST() {
  const output = await runPython('ml/utils/evaluate_model.py');
  const result = JSON.parse(output);
  
  if (result.status === 'success') {
    return NextResponse.json({
      success: true,
      data: result
    });
  }
}

export async function GET() {
  const reportData = JSON.parse(fs.readFileSync(reportPath, 'utf-8'));
  return NextResponse.json({ success: true, data: reportData });
}
```

**قابلیت‌ها**:
- اجرای ارزیابی از طریق API
- دریافت گزارش‌های ذخیره شده
- بازگشت نتایج تفصیلی

### 3. کامپوننت ModelEvaluator

**فایل**: `app/admin/ai-dashboard/components/ModelEvaluator.tsx`

```tsx
export default function ModelEvaluator() {
  const [data, setData] = useState<EvaluationData | null>(null);
  
  const runEvaluation = async () => {
    const response = await fetch("/api/admin/ai/evaluate", {
      method: "POST"
    });
    setData(result.data);
  };
  
  return (
    <div>
      {/* آمار کلی */}
      <StatsCards />
      
      {/* نمودار F1-Score */}
      <F1ScoreChart />
      
      {/* Heatmap ماتریس سردرگمی */}
      <ConfusionMatrixHeatmap />
      
      {/* پیشنهادات بهبود */}
      <ImprovementSuggestions />
    </div>
  );
}
```

**ویژگی‌ها**:
- نمایش آمار کلی مدل
- نمودار F1-Score برای هر دسته
- Heatmap ماتریس سردرگمی
- پیشنهادات بهبود مدل
- دکمه ارزیابی دستی

### 4. AI Supervisor بروزرسانی شده

**فایل**: `ml/core/ai_supervisor.py`

```python
def analyze_model_performance():
    # خواندن گزارش ارزیابی
    eval_data = json.load(open("ml/data/eval_report.json"))
    
    # تحلیل دقت مدل
    accuracy = metrics.get('accuracy', 0)
    if accuracy < 0.7:
        suggestions.append("دقت مدل پایین است. پیشنهاد: بازآموزی")
    
    # تحلیل F1-score برای هر کلاس
    for label, f1 in zip(labels, f1_scores):
        if f1 < 0.6:
            suggestions.append(f"دقت مدل در دسته '{label}' پایین است")
    
    return suggestions

def suggest_data_improvements():
    # تحلیل توزیع کلاس‌ها
    class_counts = df['category'].value_counts()
    
    for category, count in class_counts.items():
        percentage = (count / total_samples) * 100
        if percentage < 10:
            suggestions.append(f"دسته '{category}' داده کمی دارد")
    
    return suggestions
```

**قابلیت‌های جدید**:
- تحلیل عملکرد مدل
- شناسایی نقاط ضعف
- پیشنهاد بهبود داده‌ها
- تحلیل توزیع کلاس‌ها

## 📊 متریک‌های ارزیابی

### متریک‌های پایه

1. **Accuracy**: دقت کلی مدل
2. **Precision**: دقت پیش‌بینی برای هر کلاس
3. **Recall**: حساسیت مدل برای هر کلاس
4. **F1-Score**: میانگین هارمونیک Precision و Recall

### متریک‌های پیشرفته

1. **Confusion Matrix**: ماتریس سردرگمی
2. **AUC Score**: مساحت زیر منحنی ROC
3. **Cross-Validation**: ارزیابی قابلیت اطمینان
4. **Class Distribution**: توزیع کلاس‌ها

### تحلیل‌های AI Supervisor

1. **Model Performance**: تحلیل عملکرد مدل
2. **Data Quality**: کیفیت داده‌ها
3. **Class Imbalance**: عدم تعادل کلاس‌ها
4. **Feature Analysis**: تحلیل ویژگی‌ها

## 🎛️ داشبورد مدیریتی

### دسترسی: `/admin/ai-dashboard`

**بخش‌های جدید**:
- **ارزیابی مدل**: نمایش کامل عملکرد مدل
- **Heatmap ماتریس سردرگمی**: نمایش تصویری خطاها
- **نمودار F1-Score**: عملکرد به تفکیک دسته‌ها
- **پیشنهادات بهبود**: راه‌حل‌های پیشنهادی AI
- **آمار کلی**: دقت، تعداد کلاس‌ها، نمونه‌ها

### ویژگی‌های داشبورد:

```tsx
// آمار کلی
<StatsCards accuracy={metrics.accuracy} />

// نمودار F1-Score
<F1ScoreChart data={metrics.f1_scores} />

// Heatmap ماتریس سردرگمی
<ConfusionMatrixHeatmap matrix={confusion_matrix} />

// پیشنهادات بهبود
<ImprovementSuggestions suggestions={suggestions} />
```

## 📈 مثال‌های کاربردی

### 1. ارزیابی مدل

```bash
# از طریق Python
cd ml && python utils/evaluate_model.py

# از طریق API
curl -X POST http://localhost:3000/api/admin/ai/evaluate

# مشاهده نتایج
curl http://localhost:3000/api/admin/ai/evaluate
```

### 2. تحلیل نتایج

```json
{
  "status": "success",
  "metrics": {
    "accuracy": 0.875,
    "f1_scores": [0.85, 0.90, 0.80, 0.88, 0.82],
    "confusion_matrix": [[45, 2, 1], [3, 48, 2], [1, 2, 47]],
    "labels": ["anxiety", "depression", "focus"]
  },
  "suggestions": [
    "دقت مدل در دسته 'focus' پایین است",
    "افزودن داده‌های بیشتر برای دسته 'focus'"
  ]
}
```

### 3. مشاهده در داشبورد

```typescript
// در کامپوننت React
const loadEvaluationData = async () => {
  const response = await fetch("/api/admin/ai/evaluate");
  const result = await response.json();
  setData(result.data);
};
```

## 🔄 فرآیند ارزیابی

### 1. بارگذاری مدل و داده‌ها

```python
model = joblib.load("ml/core/model.pkl")
df = pd.read_csv("ml/data/user_tests.csv")
```

### 2. پیش‌پردازش

```python
X, y = preprocess_for_evaluation(df, le, scaler)
```

### 3. پیش‌بینی

```python
y_pred = model.predict(X)
y_proba = model.predict_proba(X)
```

### 4. محاسبه متریک‌ها

```python
accuracy = accuracy_score(y, y_pred)
conf_matrix = confusion_matrix(y, y_pred)
report = classification_report(y, y_pred, output_dict=True)
```

### 5. تحلیل عملکرد

```python
suggestions = analyze_model_performance(metrics)
```

### 6. تولید گزارش

```python
evaluation_result = {
    "metrics": metrics,
    "suggestions": suggestions,
    "heatmap_data": heatmap_data
}
```

## 🛠️ عیب‌یابی

### خطاهای رایج

1. **Model not found**:
```bash
cd ml && python core/train_model.py
```

2. **Data not found**:
```bash
python setup.py
```

3. **Evaluation failed**:
```bash
pip install -r requirements.txt
```

### بررسی سلامت سیستم

```bash
# تست ارزیابی
cd ml && python utils/evaluate_model.py

# بررسی گزارش
cat ml/data/eval_report.json
```

## 📊 گزارش‌های خودکار

### گزارش ارزیابی

```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "metrics": {
    "accuracy": 0.875,
    "f1_scores": [0.85, 0.90, 0.80],
    "confusion_matrix": [[45, 2], [3, 48]]
  },
  "suggestions": [
    "دقت مدل در دسته 'focus' پایین است",
    "افزودن داده‌های بیشتر"
  ]
}
```

### گزارش AI Supervisor

```json
{
  "model_analysis": [
    "دقت مدل متوسط است. پیشنهاد: تنظیم پارامترها"
  ],
  "data_improvements": [
    "دسته 'anxiety' داده کمی دارد (8.5%)"
  ]
}
```

## 🎯 نتیجه‌گیری

Testology حالا دارای یک **سیستم ارزیابی خودکار** کامل است که:

✅ **خودش خودش رو می‌بینه** و ارزیابی می‌کنه  
✅ **نقاط ضعف رو شناسایی می‌کنه** و راه‌حل پیشنهاد می‌ده  
✅ **عملکردش رو تحلیل می‌کنه** و بهبود می‌یابد  
✅ **داده‌هاش رو بررسی می‌کنه** و پیشنهاد می‌ده  
✅ **مدلش رو بهینه می‌کنه** و رشد می‌کنه  

این سیستم Testology را از یک موجود یادگیرنده به یک **موجود خودآگاه** تبدیل کرده است! 👁️🧠

---

**نکته مهم**: این سیستم به صورت مداوم مدل را ارزیابی می‌کند و پیشنهادات بهبود ارائه می‌دهد. توصیه می‌شود ارزیابی را ماهی یکبار انجام دهید.













