'use client'

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function InsightsModule() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>تحلیل و بینش</CardTitle>
      </CardHeader>
      <CardContent>
        <p>در حال توسعه...</p>
      </CardContent>
    </Card>
  )
} 