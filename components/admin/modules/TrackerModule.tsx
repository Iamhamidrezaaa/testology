'use client'

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export function TrackerModule() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>پیگیری کاربران</CardTitle>
      </CardHeader>
      <CardContent>
        <p>در حال توسعه...</p>
      </CardContent>
    </Card>
  )
} 