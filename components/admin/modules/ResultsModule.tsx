'use client'

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export function ResultsModule() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>نتایج تست‌ها</CardTitle>
      </CardHeader>
      <CardContent>
        <p>در حال توسعه...</p>
      </CardContent>
    </Card>
  )
} 