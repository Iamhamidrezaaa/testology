'use client'

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export function TestsModule() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>مدیریت تست‌ها</CardTitle>
      </CardHeader>
      <CardContent>
        <p>در حال توسعه...</p>
      </CardContent>
    </Card>
  )
} 