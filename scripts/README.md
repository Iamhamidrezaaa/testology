# اسکریپت‌های تستولوژی

این پوشه شامل اسکریپت‌های مفید برای مدیریت و import محتوا در تستولوژی است.

## 📁 فایل‌های موجود

### 1️⃣ `import-blogs.js`
اسکریپت اصلی برای import مقالات از فایل‌های Markdown به CMS

**استفاده:**
```bash
node scripts/import-blogs.js
```

**ویژگی‌ها:**
- ✅ خواندن فایل‌های Markdown از `lib/blog/articles/`
- ✅ تجزیه frontmatter (title, slug, tags, etc.)
- ✅ ارسال به API `/api/admin/blog/import`
- ✅ مدیریت خطاها و گزارش‌گیری

### 2️⃣ `update-articles.js`
اسکریپت آپدیت خودکار مقالات با frontmatter

**استفاده:**
```bash
node scripts/update-articles.js
```

**ویژگی‌ها:**
- ✅ آپدیت خودکار 50 مقاله
- ✅ تولید frontmatter کامل
- ✅ توزیع هوشمند در دسته‌بندی‌ها
- ✅ تولید تگ‌های مناسب

### 3️⃣ `seed-blog.ts`
اسکریپت seed برای ایجاد مقالات نمونه در دیتابیس

**استفاده:**
```bash
npx tsx scripts/seed-blog.ts
```

## 🚀 مراحل اجرا

### مرحله 1: آماده‌سازی
```bash
# نصب dependencies
npm install gray-matter node-fetch@2

# ایجاد پوشه media
mkdir -p public/media/blogs
```

### مرحله 2: آپدیت مقالات
```bash
# آپدیت مقالات با frontmatter
node scripts/update-articles.js
```

### مرحله 3: Migration دیتابیس
```bash
# اجرای migration
npx prisma migrate dev --name add-blog-models
```

### مرحله 4: Seed مقالات
```bash
# ایجاد مقالات نمونه
npx tsx scripts/seed-blog.ts
```

### مرحله 5: Import مقالات
```bash
# import مقالات از فایل‌ها
node scripts/import-blogs.js
```

## 📊 ساختار مقالات

### Frontmatter مورد نیاز:
```yaml
---
title: "عنوان مقاله"
slug: "slug-article"
excerpt: "خلاصه مقاله"
metaTitle: "عنوان SEO"
metaDescription: "توضیحات SEO"
tags: "تگ1, تگ2, تگ3"
category: "mental-health"
author: "تیم تستولوژی"
cover: "cover-image.jpg"
published: true
---
```

### دسته‌بندی‌های موجود:
- `mental-health` - سلامت روان (مقالات 1-10)
- `personal-growth` - رشد شخصی (مقالات 11-20)
- `relationships` - روابط (مقالات 21-30)
- `family` - خانواده (مقالات 31-40)
- `general` - عمومی (مقالات 41-50)

## 🔧 تنظیمات

### API Endpoint:
```
POST /api/admin/blog/import
```

### مسیر فایل‌ها:
- مقالات: `lib/blog/articles/`
- تصاویر: `public/media/blogs/`

### متغیرهای محیطی:
```env
DATABASE_URL="your-database-url"
NEXTAUTH_SECRET="your-secret"
```

## 🐛 عیب‌یابی

### خطاهای رایج:

1. **خطای API**: بررسی کنید که سرور در حال اجرا باشد
2. **خطای فایل**: بررسی کنید که فایل‌ها در مسیر صحیح باشند
3. **خطای دیتابیس**: بررسی کنید که migration اجرا شده باشد

### لاگ‌ها:
- ✅ موفق: مقاله آپلود شد
- ❌ خطا: بررسی پیام خطا
- 📊 خلاصه: تعداد موفق و خطا

## 📈 آمار

- **50 مقاله** آماده برای import
- **5 دسته‌بندی** مختلف
- **15 تگ** متنوع
- **سئو کامل** برای همه مقالات

## 🎯 نتیجه

بعد از اجرای کامل اسکریپت‌ها:
- ✅ 50 مقاله در CMS
- ✅ دسته‌بندی‌های کامل
- ✅ سئو بهینه
- ✅ تصاویر آماده
- ✅ API فعال
















