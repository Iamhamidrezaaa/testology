# 🧠 مغز یادگیرنده Testology

این بخش شامل سیستم هوش مصنوعی و یادگیری ماشین Testology است که قابلیت‌های زیر را ارائه می‌دهد:

## ✨ ویژگی‌های اصلی

- **پیش‌بینی شخصی‌سازی‌شده**: تحلیل رفتار کاربران و ارائه پیشنهادات مناسب
- **AI Supervisor**: تحلیل ترندها و پیشنهاد بهبود پلتفرم
- **یادگیری خودکار**: بهبود مداوم بر اساس داده‌های جدید
- **پیشنهاد محتوا**: تولید خودکار پیشنهادات محتوایی

## 📁 ساختار فولدرها

```
ml/
├── data/                   # داده‌های آموزشی و لاگ‌ها
│   ├── user_tests.csv     # نتایج تست‌های کاربران
│   └── recommendations.json # لاگ پیشنهادات
├── core/                  # هسته سیستم ML
│   ├── train_model.py     # آموزش مدل
│   ├── predict.py         # پیش‌بینی
│   └── ai_supervisor.py   # تحلیل هوشمند
├── utils/                 # ابزارهای کمکی
│   └── preprocess.py      # پیش‌پردازش داده‌ها
├── bridge/                # پل ارتباطی با Next.js
│   └── run_python.ts      # اتصال Node.js به Python
└── requirements.txt       # Dependencies پایتون
```

## 🚀 راه‌اندازی سریع

### 1. نصب Dependencies

```bash
cd ml
pip install -r requirements.txt
```

### 2. راه‌اندازی اولیه

```bash
python setup.py
```

### 3. آموزش مدل

```bash
python core/train_model.py
```

### 4. تست سیستم

```bash
python test_system.py
```

## 🔧 استفاده

### پیش‌بینی شخصی‌سازی‌شده

```python
# از طریق Python
from core.predict import predict_user_category
result = predict_user_category({
    "score": 65,
    "gender": "female",
    "age": 25
})
```

```typescript
// از طریق Next.js API
const response = await fetch('/api/ml/predict', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    score: 65,
    gender: 'female',
    age: 25
  })
});
```

### تحلیل AI Supervisor

```python
# از طریق Python
from core.ai_supervisor import analyze_trends
insights = analyze_trends()
```

```typescript
// از طریق Next.js API
const response = await fetch('/api/admin/ai/analyze', {
  method: 'POST'
});
```

## 📊 مدل‌های استفاده شده

### Random Forest Classifier
- **هدف**: دسته‌بندی کاربران بر اساس ویژگی‌های روان‌شناختی
- **ویژگی‌ها**: نمره تست، جنسیت، سن
- **دسته‌ها**: اضطراب، افسردگی، تمرکز، اعتماد به نفس، استرس

### K-Means Clustering
- **هدف**: خوشه‌بندی کاربران برای پیشنهادات گروهی
- **کاربرد**: تحلیل ترندها و رفتار کاربران

## 🎯 API Endpoints

### `/api/ml/predict`
- **روش**: POST
- **ورودی**: `{score, gender, age}`
- **خروجی**: پیشنهادات شخصی‌سازی‌شده

### `/api/ml/train`
- **روش**: POST
- **خروجی**: نتیجه آموزش مدل

### `/api/admin/ai/analyze`
- **روش**: POST
- **خروجی**: تحلیل ترندها و پیشنهادات بهبود

## 📈 داشبورد مدیریتی

دسترسی به داشبورد AI از طریق:
```
/admin/ai-dashboard
```

### ویژگی‌های داشبورد:
- سلامت کلی پلتفرم
- دسته‌های پرتکرار
- تحلیل احساسات کاربران
- عملکرد محتوا
- اقدامات پیشنهادی AI

## 🔄 فرآیند یادگیری

1. **جمع‌آوری داده**: از تست‌ها و تعاملات کاربران
2. **پیش‌پردازش**: تمیز کردن و آماده‌سازی داده‌ها
3. **آموزش مدل**: بروزرسانی مدل با داده‌های جدید
4. **ارزیابی**: سنجش دقت و عملکرد
5. **بهکارگیری**: استفاده در پیشنهادات زنده

## 🛠 توسعه و سفارشی‌سازی

### اضافه کردن ویژگی جدید

1. ویرایش `utils/preprocess.py`
2. بروزرسانی `core/train_model.py`
3. تست با `test_system.py`

### اضافه کردن دسته جدید

1. بروزرسانی `core/predict.py` - تابع `get_recommendations`
2. آموزش مجدد مدل
3. بروزرسانی داشبورد

## 📝 لاگ‌ها و مانیتورینگ

- **لاگ پیشنهادات**: `data/recommendations.json`
- **مدل ذخیره شده**: `core/model.pkl`
- **گزارش عملکرد**: در داشبورد مدیریتی

## 🔒 امنیت و حریم خصوصی

- داده‌های کاربران به صورت ناشناس پردازش می‌شوند
- مدل‌ها محلی ذخیره می‌شوند
- عدم ارسال داده‌ها به سرورهای خارجی

## 🚨 عیب‌یابی

### خطاهای رایج:

1. **ModuleNotFoundError**: نصب dependencies
2. **FileNotFoundError**: اجرای `setup.py`
3. **Model not trained**: اجرای `train_model.py`

### بررسی سلامت سیستم:

```bash
python test_system.py
```

## 📞 پشتیبانی

برای سوالات و مشکلات:
- بررسی لاگ‌ها در کنسول
- اجرای تست‌های تشخیصی
- مراجعه به مستندات API

---

**نکته**: این سیستم به صورت مداوم یاد می‌گیرد و بهبود می‌یابد. توصیه می‌شود مدل را ماهی یکبار بازآموزی کنید.













